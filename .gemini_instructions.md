# .gemini_instructions.md — NC Control Tower v2

Lee primero `AGENTS.md`.

`AGENTS.md` manda sobre cualquier otra instrucción.

No cargues `/skills` por defecto.
No leas `/_archivo_DO_NOT_READ`.
No leas `/n8n_workflows` salvo TASK_SPEC explícita.
No modifiques archivos fuera del WRITE_SET de la TASK_SPEC activa.

Los skills solo pueden usarse si la TASK_SPEC activa los incluye expresamente en READ_SET.

Antes de usar cualquier herramienta/MCP:
1. identifica TASK_SPEC activa;
2. confirma READ_SET;
3. confirma WRITE_SET;
4. confirma DO_NOT_TOUCH;
5. devuelve PLAN;
6. espera aprobación.

No construyas desde el PRD completo.
No construyas fases futuras.
